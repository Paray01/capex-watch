# Generated on every run. data.js is deliberately tracked: the Mac is the only
# machine that can reach the data sources, so it publishes the readings here.
artifact.html
alert.txt

# Local runtime state
log.txt
alerts.log
launchd.err

# Contact address for the SEC User-Agent. This repository is public.
contact.env
